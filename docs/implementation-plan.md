# Build Milestones

1. Bootstrap services and infra compose.
2. Implement auth domain and JWT lifecycle.
3. Implement upload endpoint + object storage.
4. Integrate RabbitMQ and worker consumer.
5. Generate PDF/XLSX artifacts and persist metadata.
6. Add analytics queries and RBAC isolation checks.
7. Add observability (metrics, tracing, logs).
8. Add integration tests with Testcontainers.
